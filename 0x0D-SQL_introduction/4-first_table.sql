-- Create a table called first_table in the current database
-- The database will be passed as an argument
-- The id is INT and name is  VARCHAR(256)

CREATE TABLE IF NOT EXISTS first_table(id INT, name VARCHAR(256));
